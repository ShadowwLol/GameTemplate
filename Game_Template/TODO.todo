Known bugs:
    [] Button rect is not centered with text (AKA. button.x + button.x/2 or button.x - button.x/2)

Enhancements:
    [] Add multithreading to delay spritesheet texture changes and etc.
    [] Update all coordinates from the camera's POV
    [] Don't render anything non-visible.